cmake_minimum_required(VERSION 2.8)
project(detection)
add_definitions(-std=c++11)
find_package(OpenCV REQUIRED)
add_executable(facemark faceLandmarkDetection.cpp drawLandmarks.hpp)
target_link_libraries(facemark ${OpenCV_LIBS})
